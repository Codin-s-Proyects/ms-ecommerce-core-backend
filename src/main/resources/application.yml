spring:
  application:
    name: ms-backend-core
  datasource:
    url: jdbc:postgresql://localhost:5432/ms_core_database
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
